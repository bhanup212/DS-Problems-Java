package com.bhanu;

import com.bhanu.linkedlist.MyLinkedList;
import com.bhanu.tree.BinaryTree;

import java.util.Arrays;
import java.util.Queue;
import java.util.Stack;
import java.util.Vector;

public class Main {

    public static void main(String[] args) {
	// write your code here
        int[] arr = {1,1,2,2};

        int[] biArr = {4, 2, 2, 4, 2};
        //ArrayProblem.biArray(biArr);
        //ArrayProblem.countElements(arr);
        //WindowTechnique.maxSubArraySum(arr,4,arr.length);
        //ArrayProblem.arraySorted(arr,arr.length);
        //System.out.println("element is: "+element);
        //StringProblem.reverseVowels("hello");

        /*LinkedList.Node head = new LinkedList.Node(0);
        LinkedList.Node first = new LinkedList.Node(1);
        head.next = first;
        LinkedList.Node second = new LinkedList.Node(2);
        first.next = second;
        LinkedList.Node third = new LinkedList.Node(3);
        second.next = third;
        LinkedList.Node four = new LinkedList.Node(4);
        third.next = four;
        LinkedList.Node five = new LinkedList.Node(5);
        four.next = five;
        LinkedList.Node six = new LinkedList.Node(6);
        five.next = six;

        LinkedList.printAllNodes(head);
        LinkedList.reverseKGroup(head,2);
        LinkedList.printAllNodes(head);*/

        //InsertionSort.evenOddInsertionSort(arr);

        //StringProblem.backspaceCompare("ab#c","ad#c");
        /*int value = StringProblem.myAtoi(" 42");
        System.out.println("value: "+value);*/

        //6 7 2 1 0 6
        //int value2 = linkedList.get(4);
        //System.out.println("at Index 4: "+value2);
        // 4
       /* linkedList.addAtIndex(1, 2);  // linked list becomes 1->2->3
        linkedList.get(1);            // returns 2
        linkedList.deleteAtIndex(1);  // now the linked list is 1->3
        linkedList.get(1);*/

        //BinaryTree bt = new BinaryTree();

        int index = StringProblem.strStr("mississippi","issip");

    }
}
